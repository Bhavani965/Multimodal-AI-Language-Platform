# Backend Dependencies
flask==3.0.0
flask-cors==4.0.0
gtts==2.5.1
deep-translator>=1.11.0
langdetect>=1.0.9
requests>=2.31.0
PyPDF2>=3.0.0
PyMuPDF>=1.22.0
SpeechRecognition>=3.10.0
pydub>=0.25.1
pytesseract>=0.3.10
Pillow>=10.0.0
numpy>=1.24.0
werkzeug>=3.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Audio processing
# Note: pyaudio installation varies by OS
# Windows: pip install pipwin && pipwin install pyaudio
# Linux: sudo apt-get install portaudio19-dev && pip install pyaudio
# Mac: brew install portaudio && pip install pyaudio
# pyaudio>=0.2.14

# Optional: For advanced features (comment out if not needed)
# torch>=2.0.0
# transformers>=4.30.0
# sentencepiece>=0.1.99
# openai-whisper>=20231117
# librosa>=0.10.0

# Database (for history and favorites)
# sqlite3 is built-in to Python, no need to install
